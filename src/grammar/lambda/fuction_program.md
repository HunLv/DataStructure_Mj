# 函数式编程

## 实战

## 知识

### 尾递归

起因

每次执行 Recursive方法调用都会在调用栈上创建一个新的栈帧，用于保存每个方法调用的状态（即它需要进行的乘法运算），这个操作会一直指导程序运行直到结束。  

尾调优化(tail-call optimization)介绍 TODO 不理解

> 思想
>
> > 编写阶乘的一个迭代定义，不过迭代调用发生在函数的最后（所以我们说调用发生在尾部）  
>
> 

